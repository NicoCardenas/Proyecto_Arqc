CHIP Skip {
    IN a[5], b[16];
    OUT out[5];

    PARTS:
    Register(in[11..15]=a, load=true, out[11..15]=outAdd);
    Add16(a[11..15]=outAdd, b[15]=b[15], out[11..15]=outCI);
    CI(in=outCI, out=out);
}